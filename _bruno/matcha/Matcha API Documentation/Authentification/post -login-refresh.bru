meta {
  name: post -login-refresh
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/api/login/refresh
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "refreshToken": ""
  }
}
