meta {
  name: get -tags--tagId
  type: http
  seq: 2
}

get {
  url: http://localhost:8000/api/tags/:tagId
  body: none
  auth: bearer
}

params:path {
  tagId: 
}

auth:bearer {
  token: {{token}}
}
